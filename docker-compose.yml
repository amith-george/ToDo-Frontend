services:
  todo-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: todo-frontend
    restart: unless-stopped
    ports:
      - "8081:8081"
    networks:
      - todo-net

networks:
  todo-net:
    external: true
